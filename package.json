{
  "name": "ado-link-branch-generator",
  "version": "1.0.0",
  "description": "Browser extension for generating ADO work item links and branch names",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  }
}
